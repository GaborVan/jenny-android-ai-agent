# Skills

The following skills extend your capabilities. To use a skill, read its SKILL.md file using the read_file tool.
Unavailable skills cannot be used. Dependencies are pre-installed and cannot be changed at runtime.

{{ skills_summary }}
