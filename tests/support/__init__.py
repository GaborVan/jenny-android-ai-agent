"""Helper condivisi per la suite di test (nessun test qui dentro).

Importabili da qualunque test come ``from support.<modulo> import ...``:
``tests/`` finisce in ``sys.path`` quando pytest carica il conftest di root,
lo stesso meccanismo usato da ``tests/channels/ws_test_client.py``.
"""
